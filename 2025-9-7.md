HOMEWORK

```C#
using System.Xml.Schema;

namespace _20250904
{
    internal class homework
    {
        static void Main (string[] args)
        {
            //Console.WriteLine("정수값을 입력하세요:");
            //string inputStr = Console.ReadLine();
            //// 문자열을 입력
            ////int inputValue = Console.Read(); // 한문자 입력

            //int inputValue = int.Parse(inputStr);
            //// 숫자문자열을 숫자값으로 변경.                                      
            ////int inputValue2 = Convert.ToInt32(inputStr);

            //bool ret = (0 < inputValue) && (inputValue < 10);

            //if (ret)
            //{
            //    Console.WriteLine($"{inputValue}는 0과 10사이에 있다.");
            //}
            //else
            //{
            //    Console.WriteLine($"{inputValue}는 0과 10사이에 없다.");
            //}
            //ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
            // 숙제
            // 1. 박스(0, 0) ~ (10, 10)안에 점(x,y)가 들어있는지 없는지 확인하는
            // 로직을 만드세요
            Console.Write("x값을 입력하세요: ");
            string X = Console.ReadLine();
            Console.Write("Y값을 입력하세요: ");
            string Y = Console.ReadLine();
            int x = int.Parse(X);
            int y = int.Parse(Y);
            bool x1 = (0 < x) && (x < 10);
            bool y1 = (0 < y) && (y < 10);

            if(x1)
            {
                if (y1)
                {
                    Console.WriteLine($"점 ({x}, {y})은 박스(0, 0) ~ (10, 10)안에 있다");
                }
                else
                {
                    Console.WriteLine($"점 ({x}, {y})은 박스(0, 0) ~ (10, 10)안에 없다");
                }
            }
            else
            {
                Console.WriteLine($"점 ({x}, {y})은 박스(0, 0) ~ (10, 10)안에 없다");
            }
        }
    }
}

```
