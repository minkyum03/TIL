```C#
namespace _20250919_Composition_Robot
{

    class Arm
    {
        private string _name;

        public Arm(string name)
        {
            _name = name;
        }

        public void Info()
        {
            Console.Write($"{_name}");
        }


    }

    class CannonArm : Arm
    {
        public CannonArm()
           : base("캐논암") { }

    }


    class RocketArm : Arm
    {
        public RocketArm()
           : base("로켓암")
        {

        }
    }

    class BombArm : Arm
    {

        public BombArm()
           : base("폭탄암") { }

    }

    class Robot
    {
        private string _name;
        private Arm _leftArm;
        private Arm _rightArm;

        public Robot(Arm leftArm, Arm rightArm, string name)
        {
            _leftArm = leftArm;
            _rightArm = rightArm;
            _name = name;
        }


        public void SetLeftArm(Arm leftArm)
        {
            _leftArm = leftArm;
        }
        public void SetRightArm(Arm rightArm)
        {
            _rightArm = rightArm;
        }
        public void Info()
        {
            Console.WriteLine($"-- {_name} --");
            Console.Write("왼쪽팔: ");
            _leftArm.Info();
            Console.WriteLine();
            Console.Write("오른쪽팔: ");
            _rightArm.Info();
        }
    }


    internal class Program
    {
        static void Main(string[] args)
        {
            Robot cannonArmRobot = new Robot(new CannonArm(), new CannonArm(), "Cannon Arm Robot");
            Robot rocketArmRobot = new Robot(new RocketArm(), new RocketArm(), " Rocket Arm Robot");
            Robot bombArmRobot = new Robot(new BombArm(), new BombArm(), "Bomb Arm Robot");
            Robot leftCannonArmRightRoketArmRobot = new Robot(new CannonArm(), new RocketArm(), "Left Cannon Arm Right Rocket Arm Robot");


            cannonArmRobot.Info();
            Console.WriteLine("\n");
            rocketArmRobot.Info();
            Console.WriteLine("\n");
            bombArmRobot.Info();
            Console.WriteLine("\n");
            leftCannonArmRightRoketArmRobot.Info();
        }
    }
}

//ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
 // 다형성: 동일한 메세지에 다양한 반응을 보이는 것
   // 1. 오버로딩 (overloading) : 동일한 이름의 메소드를 만드는 것
   // 2. 오버라이드 (override) : 상속관계에서 부모의 가상메소드를 자식이 재정의하는 것

   // 개발자가 되었습니다.
   // 커피숖 관리 프로그램을 제작해달라는 의뢰를 받았습니다.
   // 먼저 뭐 부터 해야 하나요?
   // 커피숖에가서 커피한잔 시키고.
   // 자리에 앉아서 커피숖의 업무흐름을 파악해야합니다.
   // 커피숖에 어떤 역활(role)과 어떤 책임(responsibility)이 있는 파악해야 합니다.

   // 커피숖에는 어떤 역활(role)이 있나요?
   // guest, 알바생, 직원, 매니저, 주문받는사람 (Cashier), 바리스타(Barista)

   // Barista(음료를 만듦), Cashier(주문을 받음, 주문을 전달, 음료전달), guest(주문을 함)

namespace _20250919
{
    class Baristar
    {
        public void MakeADrink()
        {
            Console.WriteLine($"음료를 만듭니다.");
        }
    }
    class Cashier
    {
        private Baristar _bari; // 포함, agreggation(참조)
        public Cashier(Baristar bari)
        {
            _bari = bari;
        }
        public void OrderedDrink()
        {
            Console.WriteLine($"음료주문을 받습니다.");
        }
    }

    class Guest
    {
        public void OrderingDrink(Cashier cashier) // 의존적 관계(dependancy) , 일시적 관계
        {
            Console.WriteLine($"음료를 주문합니다.");
            cashier.OrderedDrink();
        }
    }
    internal class Polymorphhism
    {
        static void Main(string[] args)
        {
            Baristar bari = new Baristar();
            Cashier cashier = new Cashier(bari);
            Guest guest = new Guest();

            guest.OrderingDrink(cashier);
        }
    }
}

// ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

namespace _202509191611_Polymorphism_CoffeeShop
{
   // 다형성: 동일한 메세지에 다양한 반응을 보이는 것
   // 1. 오버로딩 (overloading) : 동일한 이름의 메소드를 만드는 것
   // 2. 오버라이드 (override) : 상속관계에서 부모의 가상메소드를 자식이 재정의하는 것

   // 개발자가 되었습니다.
   // 커피숖 관리 프로그램을 제작해달라는 의뢰를 받았습니다.
   // 먼저 뭐 부터 해야 하나요?
   // 커피숖에가서 커피한잔 시키고.
   // 자리에 앉아서 커피숖의 업무흐름을 파악해야합니다.
   // 커피숖에 어떤 역활(role)과 어떤 책임(responsibility)이 있는 파악해야 합니다.

   // 커피숖에는 어떤 역활(role)이 있나요?
   // guest, 알바생, 직원, 매니저, 주문받는사람 (Cashier), 바리스타(Barista)

   // Barista(음료를 만듦), Cashier(주문을 받음, 주문을 전달, 음료전달), guest(주문을 함)

   public class Drink
   {
      private string _name;

      public string Name { get => _name; }

      public Drink()
      {
         _name = "음료";
      }
   }

   public class Coffee : Drink
   {
      private string _name;

      public new string Name {  get => _name; }

      public Coffee()
      {
         _name = "커피";
      }
         

   }

   public class Latte : Drink
   {
      private string _name;

      public new string Name { get => _name; }
      public Latte()
      {
         _name = "라떼";
      }

   }

   public class Tea : Drink
   {
      private string _name;

      public new string Name { get => _name; }

      public Tea()
      {
         _name = "차";
      }
         
   }

   public class Ade : Drink
   {
      private string _name;

      public new string Name { get => _name; }
      public Ade()
      {
         _name = "에이드";
      }
         
   }

   class Barista
   {
      public void MakeADrink(Drink drink)
      {
         Console.WriteLine($"{drink.Name}를 만듭니다.");
      }
   }

   class Cashier
   {
      private Barista _bari;  // 포함, agreggation(참조)

      public Cashier(Barista bari)
      {
         _bari = bari;
      }

      public void OrderedDrink(Drink drink)
      {
         Console.WriteLine($"{drink.Name}를 주문받습니다.");
         _bari.MakeADrink(drink);
      }
   }


   class Guest
   {
      public void OrderingDrink(Cashier cashier, Drink drink) // 의존적관계(dependency), 일시적관계
      {
         Console.WriteLine($"{drink.Name}을 주문합니다.");
         cashier.OrderedDrink(drink);

      }
   }



   internal class Program
   {
      static void Main(string[] args)
      {
         Barista bari = new Barista();
         Cashier cashier = new Cashier(bari);
         Guest guest = new Guest();

         guest.OrderingDrink(cashier, new Coffee());
         Console.WriteLine();
         guest.OrderingDrink(cashier, new Latte());
         Console.WriteLine();
         guest.OrderingDrink(cashier, new Tea());
         Console.WriteLine();
         guest.OrderingDrink(cashier, new Ade());
      }
   }
}





```
